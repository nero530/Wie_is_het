package MVPtest.view;

import javafx.scene.paint.Color;
import javafx.scene.shape.Rectangle;

public class Persoon extends Rectangle {
public Persoon(){

        setFill(Color.YELLOW);
        setStroke(Color.DARKGOLDENROD);
        setWidth(200);
        setHeight(200);
}

}
